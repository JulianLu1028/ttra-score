# 驗證紀錄

## 2026-09-03：GitHub Pages 示範部署準備

- 已建立長期公開 repository `JulianLu1028/ttra-score`，本機已連接同一個遠端；只提交程式、文件與虛構示範資料。
- Pages 來源設為 GitHub Actions，明確設定 repository variable `DEPLOYMENT_MODE=demo`。未設定模式時預設 production 並要求完整連線設定，未知模式與示範模式夾帶資料庫設定皆會拒絕。
- 本機整套 `npm test` 已完成：5 個測試檔、62 項測試全部通過，涵蓋先前未完成的梯次、學科與主題渲染回歸，以及新增的 8 項部署模式防護案例。
- 首次 GitHub Actions 的安裝與測試通過；因當時尚未設定 demo 變數，被正式模式防護擋下，沒有發布。第二次線上 62 項測試及模式檢查通過，型別檢查找到資料庫測試查詢回傳值缺少宣告，已補上 `heat: number` 型別，不更動 SQL 或斷言。後續發布必須通過同一工作流程的測試與 `npm run build`。
- 尚未接入正式 Supabase；示範資料重整即重設、不跨裝置／分頁同步，不可用於正式計分。正式登入、行動網路同步及負載驗收仍待賽前完成。

## 2026-09-03：檢定專區獨立配色

- 檢定家長端、裁判端與兩個操作彈窗統一為靛藍／淡紫色系；挑戰賽沿用原有綠色。沒有調整計分、權限或公布流程。
- 主題變數限制於 academic-theme 容器；彈窗另帶相同主題，避免 portal 離開容器後回到綠色。挑戰賽組件不帶此主題。
- 檢定組件與樣式的本機 HTTP 檢查均回應 200；格式檢查通過。已用程式驗證主題隔離及四組主要文字／底色對比，最低 5.20:1。
- 已新增家長／裁判同主題與挑戰賽隔離的渲染斷言；未進行瀏覽器互動或視覺 QA。
- 本次渲染測試因 worker 啟動逾時，未執行任何案例；完整 build 與獨立打包亦再次長時間停滯，已停止等待程序。此次確認範圍是本機編譯回應、格式、主題作用範圍與主要色彩對比，不能宣稱完整型別／渲染測試或 production build 通過。

## 2026-09-03：梯次、學科專區與科創 40 秒

- 已實作挑戰賽家長／裁判兩端梯次分段與篩選、全組合併名次、目前組別的人數統計、新版無學校 CSV。
- 已實作獨立學科名單、0–100 分私有草稿、授權登分、手動整批公布與再次公布更正。
- Vite production build 通過：282 個模組，主程式 gzip 約 109 KB。這次單獨執行打包，不代表下方尚未完成的 TypeScript 檢查也已通過。
- Node 原生斷言通過：各組梯次數、跨梯次合併排名、目前組別人數、科創 40 秒可接受／40.01 秒拒絕、動力 30 秒限制不變。
- 獨立 PGlite 實測通過：依序執行 001、002、003 migration，私有學科登分、公布前公開快照為空、手動公布 0 分、匿名不能讀取私有名單。
- 本機 Root 與 AcademicApp 模組 HTTP 回應 200。沒有執行瀏覽器互動或視覺 QA。
- Vitest 的 19 項資料庫案例在最後重跑皆回報通過：含學科權限、草稿隔離、整批公布、更正後再公布、防重送、版本衝突、梯次限制、學校欄位移除與原有挑戰賽計分。曾有一項測試將「成績已被更新」誤比對為「已更新」，已修正測試預期，單項重跑及全部資料庫案例均通過。
- 另已補充梯次渲染、CSV 與學科示範模式測試，但整套回歸未完成。Vitest 首次執行因 worker 啟動逾時；之後的剩餘測試及 TypeScript 檢查仍遇到 macOS 相依檔案讀取長時間停滯（程序取樣確認停於檔案 read）。已停止停滯的驗證程序，保留預覽伺服器；不可將資料庫案例通過解讀為所有測試或型別檢查皆通過。
- GitHub Pages／正式 Supabase 尚未部署，也未執行真實登入、行動網路同步與並行負載驗收。

以下保留歷史紀錄，不代表本次完整回歸已通過。

日期：2026-09-01

## 2026-09-01：單人賽調整

- 幼兒簡易機械組示範名單已清空，供 CSV 匯入測試；其餘三組各保留 6 位虛構參賽者，計分規則不變。
- 新增新 CSV 欄名、同名不同編號、舊版欄名相容，以及畫面用詞回歸測試，目前共 35 項測試（含 6 項非瀏覽器渲染檢查）。
- 本機頁面入口與更新後的 App、示範資料、匯入介面、CSV 模組 HTTP 檢查均回應 200；這不等同完整互動測試或型別檢查。
- 本次完整測試及型別檢查在相依檔案讀取時停住；改用隨附 Node 執行環境仍未完成，已停止等待程序。35 項測試尚未取得本次執行結果，不能沿用下列初版通過紀錄宣稱本次全部通過。
- 未進行瀏覽器互動或視覺 QA。

## 初版驗證（單人賽用詞調整前）

- 31 項測試通過：四組計分、同分名次、合格邊界、無效與提前終止、CSV、實際 PostgreSQL 權限與 RPC、120 筆快照、5 項非瀏覽器表單渲染檢查。
- PostgreSQL / PGlite 整合：匿名唯讀、白名單、分組裁判、報到角色隔離、重複請求、版本衝突、修改理由、內部紀錄隔離、整批匯入交易與前後端排名一致。
- TypeScript 嚴格檢查及 Vite production build 通過。
- npm audit：0 已知漏洞。
- GitHub Pages hash routing、相對資產路徑；部署流程會拒絕缺少後端設定的示範模式。
- 本機 HTTP 入口及 App 模組可正常回應。

尚未執行：正式 Supabase 專案 migration、真實 Email 登入／SMTP、兩支手機操作、瀏覽器互動與視覺 QA、公網 80–100 人並行壓力測試、GitHub Pages 正式部署。這些需正式帳號／連線設定及賽前演練後才能宣告正式環境可用。

示範模式不是正式計分資料庫，請勿於比賽當天以示範模式記錄成績。
