# 挑戰賽優化與修正計畫（2026-09-08）

## 接續工作檢查點

- 本輪授權：使用者已要求「開始優化」，並於 2026-09-08 確認舊提前終止依新規則視為未完成、繼續執行。A–D 已實作並部署；009/010 已套用正式資料庫。
- 正式專案：`https://github.com/JulianLu1028/ttra-score`。
- 主工作目錄：`/Users/julianlu/Documents/Codex/2026-08-26/ji3u/outputs/ttra-score`。
- 規劃時程式基準：`545c8bc`。檢定匯入 WHERE 問題已完成；migration 008 已套用正式 Supabase，GitHub 檢查及部署成功，勿重做。
- 此計畫只處理挑戰賽，檢定功能維持現況。
- 單人賽、編號「幼／動／程／機＋梯次字母＋三位序號」、各梯次獨立排名、名單依參賽編號排序。
- 家長姓名第二字為 `o`；現有報到時間與家長端簡化版面須保留。
- 本檔是 reset／接續工作的入口。每一階段記錄實作、測試、提交、部署及資料庫套用結果，不能把其中一項完成當作整階段完成。
- 使用者尚未要求兌換 Full reset。2026-09-08 查詢快照：用量已用 20%，可用 reset 3 次；接續時需重新查詢，不把快照當即時資訊。

## 已確認需求對照

| 編號 | 需求 | 預定階段 |
| --- | --- | --- |
| 0 | 登記成績後返回名單，自動回到原選手位置 | B |
| 1 | 未完成回合仍保存數據；調整未完成時的秒數限制 | A |
| 2 | 未完成原因改下拉選單；幼兒沒有未完成選項 | A |
| 3 | 科創取消提前終止；掉出場地、零件脫落、翻覆均為挑戰失敗 | A |
| 4 | 名單的回合進度數字加大 | B |
| 5 | 選手編號加大 | B |
| 6 | 幼兒家長端每一處球數前補「進球數」 | B |
| 7 | 裁判名單每位選手最後加入飲料「已領取」勾選框 | C |
| 8 | 「返回參賽者名單」文字加大 | B |
| 9 | 修正動力組各方向取最佳後加總及成功卻無總瓶數的矛盾 | A，依 Q1 決定 |
| 10 | 家長端顯示官方統一公告的獎狀名次，獎狀數量等待官方確認 | D，依 Q2 及官方規則決定 |
| 11 | 全部回合完成但未合格，統一顯示「未達合格標準」 | A |

## 待確認規則（不能自行推定為裁判規則）

- Q1【已回覆】：兩拉未完成、最佳推動 8 瓶判「未達合格標準」。已告知採「缺少有效方向不列合計／排名，分別顯示拉推瓶數」；拉推皆有效且任一有效回合 >=7 瓶才合格。
- Q2【已回覆】：瓶數／秒數即時顯示，只將得獎名次延後公告。
- Q3【已回覆】：未完成秒數可留空；有填則保存實際非負秒數，不受競賽時限上限限制。
- Q4【官方待定，不阻擋 A–C】：各項目／各梯次獎狀名額、同名次卡在名額邊界的處理、是否須合格才可獲獎；幼兒目前不排名，未經確認不新增幼兒名次。
- 舊資料處理【已確認／已套用】：119 位選手、218 筆回合，含 1 筆 `terminated`、18 筆 `invalid`。使用者確認旧提前終止依新規則視為未完成。009 不把 terminated 列有效；前端以未完成顯示。原始 status、score_data、revision 與稽核保留，不重寫歷史資料。
- 正式上線【已獲同意】：009/010 已成功套用正式 Supabase；不刪除名單或成績，不自動設定名額或公告。

## A．先修正回合資料與計算（最高優先）

狀態：本機已實作、回歸測試通過；009 已正式套用，舊 terminated 視為未完成。

1. 未完成時繼續顯示資料欄位，保存瓶數、計時及該組需要的數據；狀態切換不清空裁判已輸入內容。
2. 區分「原始紀錄」與「有效計分」。建議未完成的數據作為紀錄，不直接作為有效最佳回合；失敗方向對動力總分的貢獻依 Q1 決定。
3. 未完成原因選單：
   - 幼兒：不提供未完成選項；0 球仍是已記錄回合。
   - 動力、程式：超過邊界／車體鬆脫。
   - 科創：車體掉出場地／零件脫落／翻覆。
4. 簡化操作建議：一般登錄只選回合狀態；選未完成才顯示必選原因，不強制打字。首次進入不預選失敗原因，避免誤送。
5. 「未完成原因」與「更正既有紀錄的原因」分開處理，保留修改稽核，不讓選失敗原因取代說明更正原因。
6. 科創表單移除提前終止；後端同步限制新的 `terminated` 寫入。既有紀錄依唯讀盤點及確認結果處理。
7. 動力：各取最佳拉動與最佳推動後相加，同方向同瓶數取該次較短時間；不得把瓶數及秒數從不同回合拼接。缺方向、合格、排名依 Q1 對齊前後端。
8. 回合進度與合格判定分開：一般組 2 筆回合、動力 4 筆（拉 2＋推 2）。已記錄的未完成也計入進度，未送出的回合不當作失敗。
9. 建議狀態：尚未報到 → 等待挑戰 → 挑戰中（尚有回合且尚未合格）；有效成績達門檻顯示挑戰成功；全部回合完成仍未合格顯示未達合格標準。
10. 同步名單、詳情、匯出及資料庫計算，避免同位選手在不同入口顯示互相矛盾。

主要檔案：`src/ScoreForm.tsx`、`src/domain.ts`、`src/App.tsx`、`src/data.ts`、新的 Supabase migration。

現況證據：
- `normalizeScore()` 和正式 `private.normalize_score()` 在 invalid 時回傳空物件；只改表單會繼續丟失數據。
- 動力 `teamResult()` 的 qualified 目前只看任一有效回合 >=7，總瓶數則要求拉推皆有有效回合；SQL results 也有同樣分離，兩邊都須改。
- `teamResult()` 的動力 complete 目前取決於是否有有效拉推，但 SQL complete 取決於 4 筆紀錄；需統一定義。
- 科創的現有前後端都把 terminated 當有效成績，不可只刪選項。
- `RULES.md` 仍含跨梯次排名與提前終止保留得分的舊描述；實作時更新為最新已確認規則。

驗收：未完成儲存後重開仍有原數據；超過正常時限的未完成紀錄可保存但不誤成有效成績；各選单及後端一致；正常回合原有限制保留；動力邊界案例前後端相同；最後回合送出後狀態正確；更正與重送不產生重複紀錄。

## B．回到選手位置與手機可讀性

狀態：已上線並通過渲染測試。保存回到原選手的 history／篩選／scroll anchor 與 2 秒醒目；尚未實際瀏覽器互動驗收，交由使用者手機試用回饋。

1. 進入計分時記錄選手 ID、項目、梯次／篩選狀態與列表位置。
2. 返回列表待資料渲染後定位原選手；短暫醒目約 2 秒。選手因篩選消失時回到原梯次段落。
3. 處理頁內返回與瀏覽器返回；目前計分表單僅用 React selected 狀態切換，瀏覽器返回需配合 history 狀態，不能只加 scrollIntoView。
4. 保留「儲存後可繼續編輯另一回合」流程，除非使用者另要求自動返回；返回定位不等於自動跳到下一位。
5. 放大選手編號、已錄入回合數及返回名單文字，檢查手機是否擠壓計分／報到按鈕。
6. 幼兒家長名單、單回合詳情等球數前加「進球數」，不重複疊加文字。

主要檔案：`src/App.tsx`、`src/styles.css`，按需要調整 `src/ScoreForm.tsx`、摘要函式。

驗收：列表中段選手計分後返回仍在同一人；切項目／梯次不跳錯人；瀏覽器返回、非同步更新及資料量較多時正常；編號排序不變；手機文字與控制可操作。

## C．飲料領取紀錄

狀態：本機已實作，資料庫角色／組別範圍／版本衝突／取消誤勾／重送稽核測試通過。010 已正式套用；正式回滾測試通過。

1. 裁判名單每位選手最後方新增「飲料已領取」勾選框，預設未領取。
2. 狀態保存於 Supabase，可跨裝置同步；儲存中只鎖定該列控制，失敗要明確提示並恢復正確狀態。
3. 建議記錄領取狀態、操作時間、操作人及更正紀錄，允許有權限的工作人員取消誤勾。
4. 使用工作人員專用資料與受權限檢查的操作，不直接把欄位塞入公開 `teams` 而經 `to_jsonb(t)` 洩漏到家長資料。
5. 僅作人工領取紀錄，不自行從合格／排名推定可領資格；若需要資格限制，另依官方規則確認。

驗收：兩裝置顯示一致；未登入者不可讀寫；裁判組別權限生效；同列重複點擊、斷線及誤勾可處理。

## D．官方獎狀名次公告

狀態：已上線名額、單梯與全賽事統一公告預覽、人工確認、固定快照；匿名只收已公布獎狀名次。各梯名額保持空白，等待官方確認，沒有公告任何得獎者。

1. 沿用各梯次獨立排名，名單仍依選手編號顯示。
2. 工作人員預覽內部排名及待公布對象；家長僅顯示官方公布、可獲獎狀的名次。
3. 名額未設定或未經官方確認時不公布，不預填或猜測得獎名額。
4. 使用人工確認與公告快照；公告後更正須重新確認、再公布，避免官方名次隨每次登分悄悄變動。
5. 按 Q2 決定瓶數／秒數等數據在公告前是否公開。公開 API 僅回傳准許公開的結果與正式獎狀名次，不能只在畫面藏住全部排名。
6. 處理項目／梯次名額、同名次與邊界名額、版本衝突、重送及公告稽核；官方確認後才能定最終邏輯。

驗收：公告前不可從家長端取得未公布官方榜單；公告後只顯示應獲獎狀者名次；不同梯次互不混排；後續更正不改變既有公告快照，直到再次確認公布。

## 執行與用量安排

- 建議順序 A → B → C → D；A 等待規則時可先做 B。不要讓尚未決定的獎狀數量阻擋現場計分修正。
- 每階段以一組相關修改完成後再跑對應測試，避免小改一行就重跑全部。計算與權限變動必須做實質回歸測試，資料庫部分要測正式 API 條件。
- 前後端依賴變更需規劃相容的上線順序：資料庫先接受新版資料，前端再更新；確認前端能保存後才啟用新操作。不要發佈會被舊後端拒絕的畫面。
- 新 migration 的 UPDATE / DELETE 一律明確指定條件，納入 008 問題的回歸防護；不關掉資料庫保護。
- 正式測試優先使用完整回滾交易；不能把尚未提交成功的測試當作成功匯入，也不刪除現有真實資料。
- 每階段保存：已確認規則、改動檔案、測試結果、提交 SHA、部署結果、migration 是否已套用、下一個具體步驟。
- reset 前更新本檔；兌換 reset 僅在使用者明確要求時執行。reset 不應成為重做已完成工作的理由。
- 環境提醒：部分 macOS 檔案可能是 dataless 而讀取緩慢。主目錄是唯一權威來源；必要時用乾淨暫存副本與獨立 npm cache 驗證，勿把舊暫存副本當最新程式。

## 執行紀錄

- 2026-09-08：完成需求對照與原始碼檢查；已詢問 Q1、Q2，等待回覆。A–D 均未開始；正式系統未因本輪規劃而更改。
- 2026-09-08 17:19：已依 Q1–Q3 實作 A–D。本機 `npm run check` 通過：9 個測試檔、111 項測試（資料庫 32 項），TypeScript/Vite 正式建置成功。包含未完成儲存、動力缺方向、前後端比序、飲料权限／版本／稽核、公告版本／重送／隱私／整批原子性、登入切換快取隔離及 SSR 表單檢查。建置只有既有 bundle-size 與 Node deprecation 提醒。
- 測試執行於 `/private/tmp/ttra-import-fix.mUrGG9/repo`；最新改動由主目錄逐檔複製，依賴仍使用該暫存副本。主目錄是權威來源。
- 新增：`src/ChallengeStaffTools.tsx`、`tests/data-remote.test.ts`、migration `009_challenge_attempt_rules.sql`、`010_rewards_and_award_publication.sql`。修改 App、ScoreForm、domain、data、styles、RULES 及既有測試。無修改檢定流程。
- 未兌換 reset。未清除任何正式名單／分數，未套用 009/010、未 push、未公告得獎名次。
- 2026-09-08 17:37：使用者確認後，009/010 已於正式 Supabase SQL Editor 成功套用；保持 safeupdate 與既有角色權限。新增 `supabase/verify_challenge_upgrade.sql`，先經 PGlite 33 項資料庫測試，再於正式後台執行並完整回滾，PASS。正式匿名 API：scoreboard 119 位／218 筆、rank 全為 null、awards 0；飲料與名額設定端點皆 401/42501 拒絕匿名。
- 更新前完整資料摘要：teams `c4b5c56f4cbc9d58b3b13c49e4bbb166`；attempts `cb9ca784df63c1ad0755df4fb41e4ba5`。回滾腳本會逐筆 JSON 核對資料、領取及公告設定完全不變。
- 實作提交為 `4f1c33c`；本次補正式驗證腳本與部署紀錄，不改前端建置內容。
- 2026-09-08：正式發布 `c31d90e77492989ee45af2e86a62b8ee101737f4`。GitHub Actions `34211155395` 的 verify 與 deploy 均 success；本機 112 項測試通過。正式 HTML/JS 均 HTTP 200，已驗證新版失敗狀態、留空秒數、飲料端點與統一公告介面存在。
- 正式更新後資料摘要與更新前完全一致：`roster=true scores=true drinks=0 awards=0 quotas=0`。既有 119 位選手／218 筆回合原始內容均保留；舊 terminated 只改計分解讀及畫面呈現，沒有重寫原始紀錄。
- 最新入口：`https://julianlu1028.github.io/ttra-score/?v=20260908-upgrade1#/challenge/staff`。公開入口仍為 `#/challenge`。這一筆文件紀錄不變更已部署程式。

## 接續工作（本批已上線，勿重跑 migrations）

1. 官方提供各梯名額與獎狀資格後，由管理員在名次公告設定、預覽並人工确认；沒有授權不可代為公告。
2. 使用者可用手機實測返回原選手、分數保存及饮料勾選；若需處理測試分數，先取得明確的清除範圍，不能自行刪資料。
3. 不重跑 010（已建立資料表）。後續變更使用新 migration，保留正式資料及操作紀錄。
